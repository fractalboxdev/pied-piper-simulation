# Scratch / agent temp files
.tmp/

# Node
node_modules/
dist/
*.log

# Env / secrets
.env
.env.*
!.env.example
# dotenvx private decryption keys — never commit (already matched by .env.*,
# kept explicit so a future loosening of .env.* for encrypted env files
# cannot accidentally expose it)
.env.keys

# OS
.DS_Store
